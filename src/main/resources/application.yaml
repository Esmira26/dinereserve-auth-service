spring:
  application:
    name: dinereserve-auth-service

  datasource:
    url: jdbc:postgresql://localhost:5432/dinereserve
    username: postgres
    password: 1979
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false


jwt:
  secret: almararmudheyvanarasdafsghfdssfghfdssdsddsdsdsd
  access-expiration: 300000
  refresh-expiration: 1800000

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true